<p id="notice"><%= notice %></p>

  <% content_for(:pagetitle) do %>
    "<%= @sponsor.name %>" Details
  <% end %>

  <% content_for(:sidebar) do %>
    <%= render "event_list" %>
  <% end %>

<p>
  <strong>Name:</strong>
  <%= @sponsor.name %>
</p>

<p>
  <strong>Url:</strong>
  <%= @sponsor.url %>
</p>

<p>
  <strong>Logo:</strong>
  <%= @sponsor.logo %>
</p>

<br/>
<div class="btn-group">
  <%= link_to 'Edit', edit_sponsor_path(@sponsor), :class => "btn text-white btn-info" %>
  <%= link_to 'Back', sponsors_path, :class => "btn text-white btn-danger" %>
</div>